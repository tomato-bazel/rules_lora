# Allows `use_extension(..., isolate=True)` so our `crates` universe
# can coexist with consumer crates universes (rules_agentic_ide,
# prime-transformer, etc. all also register `crates`).
common --experimental_isolated_extension_usages
